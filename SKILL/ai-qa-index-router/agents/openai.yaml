interface:
  display_name: "AI 知识库索引路由"
  short_description: "按任务类型读取 QA、md、页面入口和 skill 输出源"
  default_prompt: "Use $ai-qa-index-router to choose the right local knowledge files, including page navigation maps when relevant, then verify key facts against real source or runtime evidence."
