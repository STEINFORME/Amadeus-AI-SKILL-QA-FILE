interface:
  display_name: "前端变更验证"
  short_description: "前端修改后按入口、参数、状态、交互和视觉分层验证"
  default_prompt: "Use $frontend-change-validation after frontend changes: verify entry, parameters, states, interactions, visual details, edge cases, and report validated items, unverified items, and remaining risk."
