interface:
  display_name: "md 同步 skill"
  short_description: "按 md → SKILL → runtime 方向生成、复制并校验通用 skill"
  default_prompt: "Use $skill-sync-from-md to map reusable md files to concise generic skills, sync only in the formal md to generated SKILL to runtime direction, verify hashes, skip no-drift churn, and record commit or push status."
